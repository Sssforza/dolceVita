// popup link more stage in the page services
export function moreStage () {
    $('.popupMoreStage_js').magnificPopup({
        type: 'inline',
        tClose: 'Закрыть (Esc)',
        tLoading: 'Загрузка...'
    });
}